import React, {FC} from 'react';
import {Route, Routes} from "react-router-dom";

const RoutesPage: FC = () => {
    return (
        <Routes>
            <Route path="/"/>

        </Routes>
    );
};

export default RoutesPage;